# Application Workflow Tracker Frontend

Typed React client for the mini application workflow tracker.

## Commands

```bash
npm install
npm run dev
npm run typecheck
npm run build
```

The frontend expects the backend API at `http://localhost:8000/api` by
default. Override it with:

```bash
VITE_API_URL=http://localhost:8000/api npm run dev
```
